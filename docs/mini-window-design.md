# 小窗统一设计规则（1.10.1）

适用范围：菜单栏概览、二级监控页、三级历史／控制面板；主窗口不套用小窗字号。

## 尺寸与文字

| 项目 | 固定规则 |
| --- | --- |
| 外框圆角 | 12 pt；级联相接边继续共用单条边框，接合处按实际位置收角 |
| 卡片圆角 | 10 pt，使用同一背景、边框颜色和物理像素描边 |
| 自绘控件圆角 | 6 pt；系统选择器与输入框使用同一 small 控件密度 |
| 外框内边距 | 四边 8 pt |
| 卡片内边距 | 左右 8 pt，上下 6 pt |
| 卡片间距 | 5 pt |
| 控制组行间距 | 4 pt；同行图标、文字、数值间基础间距 6 pt |
| 普通数据行 | 11 pt 系统字体，最小行高 16 pt；数字使用等宽数字 |
| 分组标题 | 11 pt 半粗体；面板标题 12 pt 半粗体 |
| 模式选择行 | 最小高度 24 pt，整行可点选 |
| 字距 | 系统默认，不按不同模块增加字距或压缩普通文字 |
| 圆环与图表 | 圆环大数字、坐标刻度保留各自语义尺寸；不与普通行混用 |

固定卡片高度是最小高度。所有二级、三级详情采用单页布局，按完整内容重新测量并增加窗口高度，不保留上下滚动区域；硬盘、网络共享盘和网络接口内部同样适用。旧的模块高度只是首次布局参考，不再截断新增行。屏幕高度足够时保持表中尺寸；只有实际可用屏幕容纳不下整页时，才整体等比适配该页，同时映射悬停位置和点击区域。禁止裁掉底部信息或单独压小某行字号。长名称可以省略并通过悬停／无障碍说明读取全文；数值单位不拆行。

能源模式与充电上限共用原生等宽分段控件，控件本身铺满卡片内容宽度；不依赖只撑大外层的 SwiftUI frame。明暗外观和禁用状态随小窗环境更新，普通刷新不触发设置提交。

## 内存组成

内存双圆下方的组成卡片使用四行容量：App、Wired、Compressed、Free；三级视图也保留四项当前百分比。Free 仅显示文字和数值，保留对齐空位，不显示颜色标记或图中的白色／灰色堆叠部分。图表只绘制 App、Wired、Compressed，颜色沿用圆环；纵轴保持 0–100%，各项比例均以总物理内存为分母，堆叠高度代表实际已用比例。复用现有时间平均规则，避免低频历史在折线中显示成离散点。自定义小窗配色也保留这三项分类色。App 沿用应用及其他占用口径，Free 沿用包含可回收缓存的可用内存口径，定义放在悬停说明中；组成互不重复，不把进程 RSS 相加作为组成。历史占比由每个采样点自身的物理内存与组成计算，缺失项不记作零。

## 风扇控制

自动、曲线、手动采用纵向单选卡片，右侧圆形标记表示当前草稿选择。仅选中的曲线或手动模式展开其控制。手动区包含同步开关、目标比例、滑杆、硬件范围与可编辑 RPM。曲线区包含预览、目标转速和编辑入口。风扇条的悬停区域继续提供转速历史，控制面板不重复同一图表。

模式选择、拖动、编辑与取消只改变草稿。仍须点击“应用”，并通过现有授权、量程、热保护和硬件回读检查才会生效；未知状态、无风扇和缺失量程不伪造可控能力。详细读数及连接处理折叠在“详细信息”中，错误与申请入口保留。

## 参考与验收边界

已查看本机 iStat Menus 7.30 的传感器设置与曲线编辑器，以及官方风扇菜单截图。采用它的纵向分组、左侧标签／右侧数值和紧凑控制理念，以上尺寸为本项目确定的规则，不声称是 iStat 的官方像素规范。

- [iStat 风扇控制说明](https://bjango.com/help/istatmenus7/fans/)
- [官方手动模式截图](https://bjango.com/images/help/istatmenus7/fan-menu-manual.png)
- [iStat 全局菜单间距设置](https://bjango.com/help/istatmenus7/global/)

本轮小窗一致性验收独立记录，不能替代原 49 项黄金视觉验收。测试通过、安装成功、运行界面审阅分别记录。
