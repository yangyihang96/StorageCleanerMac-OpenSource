# 存储清理助手 1.9.9：小窗图表验收记录

> 记录日期：2026-08-05（Australia/Sydney）
> 当前状态：**代码、Debug/Release 构建和 XCTest 已验证；1.9.9 build 202608050837 已安装并运行。缺桶补值规则已有较早同源 Release 实窗与逐像素证据；当前 build 的二/三级 Hover 复拍因 macOS `loginwindow` 锁屏拦截而未完成。**
> 证据边界：Debug fixture 只能验证布局和状态表达，不能证明 Release 数据、真实采样连续性或 iStat 行为。

## 1. 当前可证明的结论

- 跨日时间标签及相邻图表定向回归为 **90/90 通过**，其中 `PanelChartNormalizationTests` 为 **44/44 通过**。
- 最终 Debug 全量复跑为 **2160 tests、5 skipped、0 failures**；5 个 skip 均为显式 opt-in 的真实清单、能耗或硬件/优化验证。
- 当前代码使用带时间戳的样本、固定时间桶、共享时间锚和共享历史 Store；不是用当前瞬时值回填过去。
- 网络图现在每帧只准备一次日期范围与上传/下载共享桶，量程、绘制、Hover 和辅助功能复用同一份结果；长范围共享时钟只在合理桶边界推进，真实新样本仍立即刷新。
- 当前 Release 安装包位于 `/Applications/存储清理助手.app`（版本 `1.9.9 (202608032213)`，主程序 SHA-256 `8adf66fda6188bafd8579e407c7c07fd272e72b2d5ac7b74971882694113ec79`）。安装后主程序与新 Release 源产物哈希一致，deep/strict 签名通过；最终哈希一级窗实拍见 `docs/screenshots/1.9.9-installed-8adf66fd/menu-panel-level1-current.png`。
- 图表可见跨度 **超过 24 小时** 时，时间轴与 Hover Tooltip 共用本地化时间格式，中文显示为“日期＋上午/下午＋时分”（例如 `8月3日 下午 3:20`）；恰好 24 小时及更短范围保留简洁时间/相对刻度。跨日轴使用绘图区内三等分布局，未改变 time-to-x 映射。
- `docs/screenshots/1.9.9-installed-277dca95/menu-panel-level3-cpu-3days-current.png` 是真实安装版的 3 天 CPU 三级现场，轴上清楚显示 `7月31日 下午 9:58`、`8月2日 上午 9:58`、`8月3日 下午 9:58`。它来自 build `202608031808` / SHA `277dca95…`，紧邻最终构建且图表源码相同；因此可证明跨日轴的真实显示，但不能冒充最终 SHA 的 Hover 交互证据。
- iStat 真实菜单截图已完成 2 pt 柱宽、1 pt 间距、约 120/96 槽以及 1h/1d/3d 相邻桶时间的像素/悬停测量；动态刷新、睡眠/唤醒和关闭重开的逐帧录屏仍待补，详见 `docs/1.9.9-istat-chart-observation.md`。

## 2. 测试结果

| 范围 | 结果 |
|---|---|
| `PanelChartNormalizationTests` | 44/44 通过 |
| 跨日标签及电池/悬停相邻图表定向回归 | 90/90 通过 |
| SwiftPM 全量回归 | 2160 tests、5 skipped、0 failures |

测试覆盖不能替代真实窗口截图；最终哈希的二/三级 Hover 仍需在用户会话解锁后复拍。

## 3. 当前实现合同

| 项目 | 当前代码事实 | 证据位置 |
|---|---|---|
| 时间轴 | `MenuBarTelemetryPoint` 和磁盘/电源点均带真实 `Date`；样本按时间进入固定槽位 | `MenuBarTelemetryModels.swift`、`MenuBarGeekCharts.swift` |
| 当前窗口 | 小窗默认 120 秒；历史范围为 1h、3h、6h、12h、1d、3d、7d、14d、28d | `GeekChartWindow.defaultDuration`、`GeekChartRange` |
| 历史保留 | 最多 28 天；最近 1 小时保留高分辨率，较老数据按 60 秒桶保留桶内最新样本 | `MenuBarHistoryRetention` |
| 柱几何 | 2 pt 柱宽、1 pt 间距、3 pt pitch；槽数由绘图区宽度计算 | `MiniWindowStyleTokens`、`GeekChartWindow.barSlotCount` |
| 每柱时长 | `duration / visibleSlotCount`；例如 360 pt 历史绘图区对应 120 个槽位 | `GeekChartWindow.barBucketDuration`、`historyWidth` 注释 |
| 最新方向 | 时间越新 x 越大，最新有效桶位于右侧 | `GeekChartWindow.barSlot` / `barX` |
| 桶聚合 | 柱图取桶内最后一个真实样本；CPU 堆叠使用同一个 telemetry snapshot | `TimeBucketAggregator.lastValid` / `lastSample` |
| 缺失数据 | Store 不插值、不写入 0 或合成样本；图表展示层在首末真实桶之间建立连续槽，并用最近真实邻居补全内部空槽（等距优先前一个），Hover 以 `≈` 和“邻近样本补全”标明；真实 0 保持实测 0 | `GeekChartWindow.barBucketSnapshots`、`TimeBucketAggregator.nearestFilled` |
| 刷新 | 真实新样本立即刷新；共享空闲时钟按最多 120 个可视槽位推进（120 秒范围为 1 秒、1 小时为 30 秒、1 天为 12 分钟）；减少动态、屏幕休眠或 critical thermal 时暂停 | `ChartActivityPolicy`、`GeekLiveChartTimeline` |
| 动态量程 | 网络/磁盘立即扩张，下降按 10 秒稳定衰减；百分比保持 0…100 | `PanelChartAxisScaleState`、`ChartDomainPolicy` |
| 系统时间变化 | 共享锚比较 wall 与 monotonic elapsed；漂移达到 2 秒才开启新 epoch 并 re-anchor，正常小漂移不抖动 | `PanelChartTimeAnchor`、`PanelChartClock` |
| 睡眠/唤醒 | 唤醒后的显著 wall/monotonic gap 进入新 epoch；历史 Store 不清空、不补写样本；可见内部空槽仅在绘制时标记为邻近补全 | `PanelChartClock`、`MenuBarTelemetryHistory` |
| 乱序样本 | 倒退时间戳保留并进入新 epoch；forward gap 保留在原始历史中，可见内部槽由展示层补全且不冒充实测 | `MenuBarTelemetryHistory.epochBreaks` |
| 高频历史容器 | 固定容量 RingBuffer，append/覆盖 O(1)，读取按 oldest → newest；只用于共享 telemetry 热路径 | `RingBuffer`、`MenuBarTelemetryHistory` |

## 4. 各图表代码验收

| 图表 | 横轴与聚合 | Y 轴 / 方向 | 颜色或系列 | 当前代码级结论 |
|---|---|---|---|---|
| CPU | 默认 120 秒；桶内同一 snapshot；右侧最新 | 0…100%；用户 + 系统堆叠向上 | 用户 / 系统两色 | 堆叠与 headline 可来自同一历史点；真实负载截图待补 |
| 内存 | 默认 120 秒；桶内最后有效值 | 0…100%；单系列向上 | 内存色 | 不把瞬时值重复成历史；真实压力/交换现场待补 |
| 网络 | 默认 120 秒；上传/下载共享时间桶；桶内最后有效值 | 中间零轴；上传向上、下载向下；共享动态上限 | Upload / Download | 空闲仍可保留基线；真实同时上传下载待补 |
| 磁盘 | 默认 120 秒；读写共享时间桶；桶内最后有效值 | 中间零轴；读取向上、写入向下；共享动态上限 | Read / Write | 单位为 bytes/s；真实磁盘负载待补 |
| 电池 | 电量桶取最后有效值；电源时间线复用同一连续桶/x；内部缺槽复用同一个邻近真实电量与供电状态 | 电量固定 0…100%；补全桶不改 Store | 外接电源蓝；电池粉；未知中性/不绘制 | 不从预计时间生成趋势；补全柱与供电轨在同一 x 切换；真实充放电长序列待补 |
| GPU | 带时间戳的 GPU 样本；桶内最后有效值 | 0…100%；当前详情为折线 | GPU 色 | GPU 不可用时隐藏/采样占位；真实 GPU 负载待补 |
| 温度 | 带时间戳的芯片温度；桶内最后有效值；内部缺槽使用最近真实邻居 | 固定 20…110 °C；当前详情为折线 | 警示温度色 | 展示补全不写回历史，Hover 明示；真实传感器序列待补 |
| 风扇 | 带时间戳 RPM；桶内最后有效值 | 硬件范围派生；Hover 可用柱图，三级详情为折线 | 风扇中性色 | 无传感器时不显示假历史；真实多风扇序列待补 |

## 5. 连续性与布局测试覆盖

当前定向回归覆盖了规则/不规则采样、缺失值、重复和乱序时间戳、未来时钟容差、固定桶、时间范围持久化、历史压缩、网络/磁盘双向零轴、电池充放电颜色与同 x 时间线、Panel 放置/重开、Hover corridor、窗口尺度、2 pt / 1 pt 几何合同，以及 1 小时、恰好 24 小时、超过 24 小时、中英文和午夜跨日格式。

这能证明测试输入下的代码行为，但不能替代：

- 面板连续打开数小时；
- 物理睡眠/唤醒；
- 真实系统时间调整；
- 不同显示器缩放；
- Release 安装版的八张真实图表截图。

## 6. 截图验收表

### 6.1 前一安装哈希的 CPU 三级链路截图

- `installed-current-c90304a6/processor/01-level1.png`：一级摘要；
- `installed-current-c90304a6/processor/02-level2-cpu.png`：一级 + 二级 CPU；
- `installed-current-c90304a6/processor/03-level3-cpu-history-tooltip.png`：三级 CPU 历史与真实 Tooltip；
- `installed-current-c90304a6/processor/04-level3-combined.png`：三级 CPU 历史 + 二级 CPU + 一级摘要；
- `installed-current-c90304a6/power/03-level3-power-history.png` 与 `04-level3-power-electrical.png`：当前 100% 已充满、外接电源状态下的历史和电气信息。

CPU 组合截图为 2020 × 1106 px（Retina 2×），对应 1010 × 553 pt；横向宽度为 `384 + 5 + 312 + 5 + 304 pt`。两处可见间隔均为 5 pt，三层无覆盖。三级历史图当次显示 1 小时并取得真实 Tooltip；历史空白不插值，也不用当前值回填。它只证明 `c90304a6…f3a4` 安装包当时的现场，不是最终哈希的交互证据。

### 6.2 最终安装哈希与跨日实拍

- `docs/screenshots/1.9.9-installed-8adf66fd/menu-panel-level1-current.png`：最终 Release build `202608032213`、SHA `8adf66fd…ec79` 的真实一级小窗；
- `docs/screenshots/1.9.9-installed-277dca95/menu-panel-level3-cpu-3days-current.png`：紧邻最终构建前、图表源码相同的 build `202608031808` / SHA `277dca95…` 真实 3 天三级窗；可见 `7月31日 下午 9:58`、`8月2日 上午 9:58`、`8月3日 下午 9:58`；
- 最终哈希的二级/三级 Hover、Tooltip 和跨 gap corridor 实拍：**未取得**。macOS `loginwindow` 锁屏拦截鼠标 Hover；没有绕过锁屏，也没有把旧哈希或 fixture 冒充最终哈希现场。
- `menu-panel-level3-cpu-3days-tooltip-current.png` 未形成肉眼可见的 Tooltip，因此不计为 Tooltip 验收证据。

### 6.3 前一安装哈希的六组真实三级截图

目录：`docs/screenshots/1.9.9-small-window-charts/installed-current-c90304a6/`。保存 CPU、内存、网络、磁盘、电池、温度和风扇的二/三级阶段；一级摘要位于最右，二级与三级依次向左，Panel placement 使用 5 pt 间隔。该目录现在只作为前一安装哈希证据。

当前偏好保存的通用历史范围为 3 天，电池功率三级页为 14 天；截图保留真实空桶，没有为了验收改成 1 小时、回填历史或插值。CPU 三级图显示 1888/1902 个有效样本；电池二级图显示 100% 已充满、外接电源蓝柱/蓝色时间线、100% 洋红健康环和三条真实显著能耗应用。

### 6.3 当前应用自身能耗复核

使用同一台 Mac、同一已安装测试包，以进程累计 CPU 时间做 20 秒窗口复核：

- 修复前仅打开一级菜单：增加 11.03 CPU 秒，约等于单核 55.2%；
- 仅合并网络图重复计算后：增加 9.24 CPU 秒，约 46.2%；
- 最终一级菜单：增加 2.44 CPU 秒，约 12.2%；
- 最终三级菜单全部可见：增加 3.54 CPU 秒，约 17.7%；
- 不打开菜单时：增加 0.67 CPU 秒，约 3.35%。

采样栈确认热点是 SwiftUI 图表重绘以及网络图对同一历史的重复排序/分桶，不是截图循环；`NativeNetworkProcessService` 当时主要处于等待，未从显著能耗列表过滤 Storage Cleaner 自身。上述数字是短时本机进程测量，不等同于 Activity Monitor 的长期“能源影响”评分。

| 图表 | 目标路径 | 真实 1.9.9 截图 | 横轴/每柱说明 | iStat 相同点/差异 | 连续性实机结论 |
|---|---|---|---|---|---|
| CPU | `installed-current-c90304a6/processor/04-level3-combined.png` | 前一安装哈希、实时 CPU 与 Tooltip | 三级 1h；2 pt 柱 + 1 pt gap；最右为现在 | 静态几何、右侧最新、堆叠和固定桶对齐 | 前一哈希 Hover 通过；最终哈希锁屏待补 |
| 内存 | `installed-current-c90304a6/memory/03-level3-memory.png` | 当前压力/使用率、进程与 Swap | 固定 0…100 | iStat 的压力/构成/Swap 实测为固定桶；本项目系列结构不同 | 真实 Hover 三层加载通过；睡眠/长时间待验证 |
| 网络 | `installed-current-c90304a6/network/03-level3-network.png` | 当前 Wi-Fi、实时上传/下载、地址和进程 | 双向零轴 + 固定时间槽 | iStat 实测上传/下载共享零轴；本项目同语义 | 真实 Hover 三层加载通过；持续双向负载待验证 |
| 磁盘 | `installed-current-c90304a6/disk/03-level3-disk.png` | 当前卷、实时读写和进程 | 读写共享零轴 | iStat 实测读写共享零轴；本项目同语义 | 真实 Hover 三层加载通过；受控持续 I/O 待验证 |
| 电池 | `installed-current-c90304a6/power/03-level3-power-history.png` | 100% 已充满/外接电源、100% 健康和真实能耗列表 | 二级预览 + 三级 7d；蓝/粉状态柱及供电轨 | iStat 实测约 96 槽和供电轨；本项目严格区分 missing | 已充满真实 Hover 通过；充放电切换待验证 |
| GPU | `installed-current-c90304a6/sensors/02-level2-sensors.png` | 当前 GPU 温度/使用率在二级页可见；独立历史未采集 | 当前状态来自实时快照 | iStat 使用单系列柱；本项目详情结构不同 | 状态截图已有；独立历史截图待补 |
| 温度 | `installed-current-c90304a6/sensors/03-level3-temperature.png` | 当前 CPU/GPU/区域温度与悬停详情 | 固定温度域；缺口断开 | iStat 使用单系列柱；本项目保留折线/事实卡 | 真实 Hover 加载通过；受控热负载待验证 |
| 风扇 | `installed-current-c90304a6/sensors/04-level3-fan-control.png` | 当前两把风扇实时 RPM 与 fail-closed 控制入口 | 硬件范围派生；不伪造可写能力 | iStat 使用单系列柱；本项目额外有安全控制层 | 只读入口通过；写入/恢复未验证 |

每张截图补入后还需记录：运行构建、窗口尺寸、显示缩放、采样起止时间、实际槽数、纵轴、颜色、聚合规则、缺失值表现和截图是否来自真实数据。

## 7. 当前未通过的验收门

1. iStat 动态刷新、睡眠/唤醒、关闭重开和不同宽度的逐帧录屏；静态像素/时间桶测量已完成；
2. 独立 GPU 历史绘图区、电池充电/放电切换长序列和 Developer ID/公证发布包截图；
3. 睡眠/唤醒、关闭重开、不同宽度和长时间运行；
4. 当前源码 Release App/DMG/ZIP 已本机构建、签名和校验成功，build `202608032213` 已安装；`spctl` 因未公证而拒绝，Developer ID notarization 与最终哈希的解锁交互截图仍未完成；
5. 真实网络双向、磁盘读写、GPU 负载、充放电和多风扇场景。

## 8. 跨日时间标签增量验证

- `PanelChartDateFormatting` 以 `visibleDuration > 86_400` 作为唯一日期显示门；CPU/内存、网络、磁盘与电池 Tooltip 均显式传入各自真实可见跨度。
- 定向回归：`PanelChartNormalizationTests` **44/44**；与电池/悬停相邻套件联合为 **90/90**。覆盖 1 小时、恰好 24 小时、24 小时加 1 秒、3 天、7 天、中英文 locale 和午夜跨日。
- Release App/DMG/ZIP 均由含本修正的最终源码重新构建；四份归档 checksum、ZIP 解包、DMG 校验与应用签名均通过。
- 真实 3 天轴已在图表源码相同的 build `202608031808` / SHA `277dca95…` 中拍到，标签为 `7月31日 下午 9:58`、`8月2日 上午 9:58`、`8月3日 下午 9:58`。最终 build `202608032213` / SHA `8adf66fd…ec79` 的一级窗已实拍，但二/三级 Hover 复拍被 macOS `loginwindow` 锁屏拦截；真实 Tooltip 及 7 天轴仍未验证。

## 9. 当前安装构建的跨日标签复核（2026-08-04 02:48 AEST）

- 日期门保持为严格的 `visibleDuration > 86_400`：恰好 24 小时仍只显示时间，24 小时加 1 秒开始显示本地日期、上午/下午和时间，例如“8月4日 下午 3:20”。
- 坐标轴和 Hover Tooltip 共用 `PanelChartDateFormatting`，不再各自格式化；3 天、7 天和午夜跨日均读取同一规则。
- 精确定向测试 **2/2 通过**；时间序列/系统监控相邻回归 **86/86 通过**；最终全量 XCTest **2193/9 skipped/0 failures**。
- 含该规则的 Release 已安装为 1.9.9 build `202608040132`，主程序 SHA-256 为 `365aa9394c353156040c47a6ffdd53bfd170518792d346babc983198b910e8ec`，deep/strict 签名通过。
- 当前 `IOConsoleLocked = true`；因此仍没有当前哈希的多日二/三级窗口与 Tooltip 截图。此项保留为未验证，不复用旧哈希截图冒充当前现场。

## 10. build 202608040340 当前代码与安装验证

本节取代前文的“当前构建”口径。

- 日期门仍严格使用 `visibleDuration > 86_400`。超过一天时，坐标轴与 Tooltip 同时显示本地日期、上午/下午和时间；恰好 24 小时不显示日期。跨午夜、多日和 Tooltip 共用格式化器的 `PanelChartNormalizationTests` 为 **48/48**。
- 父子 Panel 保持 4–6 pt 可见间隔；`MenuBarPanelSession` 的方向安全三角只参与 screen-space Hover 判定，不创建透明点击窗口。包括斜向进入偏移详情窗在内的相关测试为 **33/33**。
- 最后一次全量 XCTest 为 **2203/10 skipped/0 failures**。包含上述代码的 Release 已安装为 **1.9.9 (202608040340)**，主程序 SHA-256 `a391237c2565c6e87115f22026e28c684082d47603f6ed095c20e4eefbc4a098`，deep/strict 签名通过。
- 显式 UI smoke 复跑因控制台锁定而跳过；所以当前构建仍没有真实多日轴、Tooltip、安全三角 Hover、1h/6h 切换或浅/深色录屏。旧哈希截图继续只作历史参考，不能代替本构建现场。

## 11. build 202608040519 真实历史流与安装验证

本节取代前文“当前构建”口径。

- 跨日门仍严格为 `visibleDuration > 86_400`；超过一天时坐标轴和 Tooltip 共用“日期 + 上午/下午 + 时间”，例如“8月4日 下午 3:20”，恰好 24 小时仍只显示时间。
- GPU 温度新增独立历史通道，只从真实 `.gpu` 温区采样；旧 JSON 解码为缺失，缺传感器时不生成数据。三级温度图按真实可绘制序列显示芯片/GPU 图例、统计和 Tooltip。
- 内存从通用 monitor 流拆出独立 `memoryTelemetry`：使用 `MemorySnapshot.generatedAt`，保存使用率、压力（`100 - pressure headroom`）、压缩内存与 Swap。通用 monitor 点不再把最近内存读数写成新历史；missing 保持 nil，真实 0 保留，不插值、不回填。
- 主窗/二级内存图读取真实内存流；三级将使用率+压力放在固定 0–100% 图，将压缩内存+Swap 放在独立 bytes 图，避免不同单位共用错误量程。持久化兼容旧 JSON并支持重启恢复。
- 根代理联合回归 **227/3 skipped/0 failures**；最终全量 XCTest **2213/13 skipped/0 failures**，完整日志未检出 warning/error/fatal。Debug 和 Release 均成功。
- 当前安装为 **1.9.9 (202608040519)**，主程序 SHA-256 `0bb727094342dd1d0032b719fa0049e66160a47e6a82e5e977d958f23b147957`，deep/strict 签名通过。锁屏下 20 秒无面板 CPU 时间增加 `0.28 s`，约单核 `1.4%`。
- 显式 UI smoke 仍因系统锁屏明确跳过；当前哈希的多日轴、GPU/内存 Tooltip、三级 Hover、浅/深色与 1h/6h 录屏继续属于未验证现场，不以旧截图替代。

## 12. build 202608040614 跨日日期与时段最终复核

- 日期门保持为严格的 `visibleDuration > 86_400`。超过一天后，中文横轴起点、中点、终点与 Hover Tooltip 共用 `PanelChartDateFormatting`，统一显示“日期 + 上午/下午 + 时间”，例如 `8月3日 下午 3:20`；午夜与中午分别验证为 `8月4日 上午 12:05`、`8月4日 下午 12:00`。
- 恰好 24 小时仍保留既有相对轴，不增加日期；24 小时加 1 秒、3 天和 7 天开始显示日期。`PanelChartNormalizationTests` 共 **48/48 通过**，其中跨日格式和横轴/Tooltip 同源断言 **2/2 通过**。
- 最后一次全量 XCTest 为 **2225 项执行、13 项跳过、0 failures，47.306 秒**；Debug bundle-only 与 Release 均成功，日志未检出 warning/error/fatal。
- 当前安装为 **1.9.9 (202608040614)**，主程序 SHA-256 `2eb0f966603f23bb33d583b52ad2f2464e744aba953921bc6acb0bd95362b24f`，deep/strict 签名通过。
- 当前控制台仍锁定；因此本构建没有新增真实多日轴或 Tooltip 截图。旧构建实拍只保留为历史参考，不冒充当前哈希现场。

## 13. build 202608040743 当前安装复核

- 日期门继续严格使用 `visibleDuration > 86_400`；超过一天时，横轴与 Hover Tooltip 同时显示本地日期、上午/下午和时间。针对性回归 **2/2 通过**。
- 真实 3 天截图仍可证明布局和文案，标签为 `7月31日 下午 9:58`、`8月2日 上午 9:58`、`8月3日 下午 9:58`；它来自图表源码相同的历史安装 build，不冒充当前 SHA 的 Hover 证据。
- 最终全量 XCTest **2231 项执行、13 项环境相关跳过、0 failures，48.107 秒**；Release 和四份归档验证通过。
- 当前安装为 **1.9.9 (202608040743)**，主程序 SHA-256 `ddc3140041673e2bbe2d95e588840bfee7880e7c2a444a020bfe5e07c37fa6e4`，deep/strict 签名通过。
- `IOConsoleLocked = Yes`；当前哈希的多日轴、Tooltip、浅/深色与 Hover 录屏仍未重新拍摄。

## 14. build 202608041050 多分辨率持久化边界

- `MetricHistoryStore.multiresolution` 原先只比较整数 bucket index。不同分辨率在构造出的日界线碰撞点可能把两条真实样本误认为同桶并覆盖；现在 bucket identity 同时包含 `bucketDuration` 和 `bucketIndex`，进入高分辨率段时也明确重置桶状态。
- 新回归 `testUnifiedMetricHistoryKeepsSamplesAcrossResolutionBoundary` 在旧逻辑会丢一条样本；当前历史存储 **9/9**、图表/Benchmark/风扇/历史组合 **97/97**、全量 **2251/10 skipped/0 failures**。
- 当前 Release 菜单栏小窗已实拍，真实 CPU、网络与磁盘历史可见。该截图不证明所有八类图表的长时间连续性，也不替代 1h/6h、多日 Tooltip、睡眠唤醒和完整 Hover 视频。

## 15. build 202608041243 跨日实机最终证据

- 当前安装 Release 的 3 天 CPU 历史已重新实拍，不是 Debug fixture。横轴显示 `8月1日 下午 1:01`、`8月3日 上午 1:01`、`8月4日 下午 1:01`；Hover Tooltip 显示 `8月4日 上午 5:13`。
- 证据截图为 `artifacts/1.9.9-build202608041243/screenshots/cpu-history-3-days-tooltip-tight-release.png`；完整三级级联为同目录 `cpu-history-3-days-tooltip-release.png`。
- 坐标轴和 Tooltip 继续共用 `PanelChartDateFormatting`。规则是可见跨度严格大于 24 小时时显示“日期＋上午/下午＋时间”；24 小时及以下保持简洁时间。
- 跨日格式与轴/Tooltip 同源断言 **2/2 通过**；最终全量 XCTest **2257 项执行、10 项明确跳过、0 failures，52.696 秒**。
- 当前截图证明 Release 现场的三级加载、3 天选择、跨日横轴和 Tooltip；1h/6h 切换视频、完整跨窗 Hover 视频、浅色/深色对照、睡眠唤醒和 7/28 天长时连续性仍未验证。

## 16. build 202608041243 全指标当前安装版补拍（2026-08-04 14:05 AEST）

- 从唯一安装路径 `/Applications/存储清理助手.app` 运行的 Release 补拍 15 张窗口级 PNG（13 张小窗指标矩阵 + 2 张深色主窗/CPU 级联），目录为 `artifacts/1.9.9-build202608041243/screenshots/chart-matrix-live-20260804/`。捕获时主程序仍为 build `202608041243` / SHA-256 `353a99329d810a2e1c84fb559049228daa3c581aa655cbcedbc147783346d670`，没有替换应用或使用 Debug fixture。
- 当前哈希已补齐：一级摘要、CPU 二级、内存二级、网络二级、磁盘二级、传感器二级、温度三级、GPU 三级、风扇三级、电池二级，以及电池 1 小时、3 小时和 6 小时历史。窗口捕获保留真实 5 pt 父子间隔，不合成面板。
- 电池现场为 **100%、已充满且仍接通外部电源**，因此电量柱和供电时间线均为真实蓝色平台；健康度为 100% 洋红色环。这不证明放电、粉→蓝或蓝→粉切换，也没有为截图生成假历史。
- 风扇现场显示两把风扇的实时 RPM、硬件最小/最大范围和历史；界面仍如实显示“未验证”。未迁移旧 helper，未触发管理员批准，未写入 RPM。
- 窗口级视频 `14-cpu-live-refresh-8s-window.mov` 为 H.264，1242×1106，8.008 秒，不包含桌面背景；首尾真实帧中 CPU User/System 从 `12%/15%` 变为 `11%/8%`，GPU 从 `47%` 变为 `70%`，网络上传从 `5 KB/s` 变为 `4.3 MB/s`。它证明窗口可见时实时快照持续刷新，不替代睡眠/唤醒或长时运行证据。
- 应用自身外观偏好原值为 `system`。验收时临时切到 `dark`并重启同一安装包，实拍 `15-main-dark-live.png` 和 `16-cpu-level2-dark-live.png`；随后已恢复、回读 `system` 并再次启动应用。这证明当前主窗与 CPU 级联在浅/深外观下可用，不代表全部功能页已做完整浅/深矩阵。
- 仍未验证：保留式 1h↔6h 选择操作录屏（当前只有两张独立真实截图）、完整一级→二级→三级 Hover 录屏、全页面/全指标浅深对照、睡眠/唤醒、关闭重开、不同窗口宽度、电池充放电转换和风扇写后回读。

## 17. build 202608041555 当前安装版缺口补全复核（2026-08-04 16:05 AEST）

- 当前安装 Release 为 `1.9.9 (202608041555)`，主程序 SHA-256 为 `0787ae80091517a54cc6657efdeb79fc2d111f5e699246660b3d1870429d157b`；截图来自唯一安装路径 `/Applications/存储清理助手.app`，不是 Debug fixture，也没有另开第二实例或终止正在运行的实例。
- 当前真实截图目录：`artifacts/1.9.9-build202608041555/screenshots/chart-gap-fill-20260804/`。可见证据包括：一级实时摘要（`01-release-level1.png`）、电池二级（`02-release-battery-level2.png`）、电池三级 1 小时连续柱（`03-release-battery-level3-filled.png`）、内部缺口最近邻补全 Tooltip（`04-release-battery-estimated-tooltip.png`）、时间范围菜单（`05-release-battery-range-menu.png`）、7 天跨日/状态柱（`06-release-battery-7day-filled.png`）、7 天日期 Tooltip（`07-release-battery-7day-estimated-tooltip.png`）和 CPU 三级 3 天实时柱（`08-release-cpu-level3-alive.png`）。
- `04` Tooltip 肉眼可见“≈ 100%”与“邻近样本补全”，日期为 `下午 3:42:12`；`07` Tooltip 肉眼可见 `8月2日 上午 5:16`、`≈ 100%` 和同一补全说明。`05` 现场显示 1 小时、3 小时、6 小时、12 小时、1 天、3 天、7 天范围菜单；`06` 显示 7 天真实历史中的蓝色外接电源段、粉色电池段和同 x 供电轨。
- 该现场仍是 100% 已充满且接通外部电源的真实机器状态；因此证明了电量柱/供电轨的时间轴和补全标识，不证明放电或充放电切换。`08` 证明当前 Release 的 CPU 用户/系统共享槽位、右侧最新方向和实时三级加载；未将其扩大为其他指标的实机覆盖。
- 定向图表/电池/悬停/历史/面板回归：**103/103 通过**，其中 `PanelChartNormalizationTests` **50/50**、`BatteryPresentationTests` **5/5**、`GeekSensorsPowerHoverDetailTests` **19/19**、`MenuBarPowerHistoryStoreTests` **9/9**、`UnifiedMenuBarPanelTests` **20/20**。`git diff --check` 通过。
- 该批证据不改变未验证边界：睡眠/唤醒、关闭重开、不同窗口宽度、长时连续录制、CPU/网络/磁盘/内存/GPU/温度/风扇全指标当前 hash 矩阵、充放电转换和完整跨窗 Hover 录屏仍未取得；旧 hash 截图继续只作历史参考。

## 18. build 202608041745 邻近补全同可见度复核（2026-08-04 17:54 AEST）

- 当前安装 Release 为 `1.9.9 (202608041745)`，主程序 SHA-256 `f279d62f9354a0faeb3858988087012f6e34342cc31a4ef960eb1be2f94ed7a1`，deep/strict 签名、DMG/ZIP checksum 和 DMG verify 均通过。旧 build `202608041705` 完整保存在 `/private/tmp/storage-cleaner-install-backup-202608041745/存储清理助手.app`，未发布 1.9.9。
- 共享 `TimeBucketAggregator.nearestFilled` 只在显示桶中补值，不写回历史 Store；等距时取左侧最近真实样本，真实 `0` 与 missing 继续严格区分。CPU/内存/风扇堆叠柱、网络、磁盘、电池/电池功率以及折线历史均已复核走该共享入口。
- 补齐柱不再因 `isEstimated` 被降到 68%–72% 透明度，视觉不再出现近似白色的竖向弱柱；估算语义仍保留在 Tooltip 的 `≈` 和“邻近样本补全”中。采集开始前、最后真实样本后的边界，以及整个范围完全无真实邻居时仍不外推，避免伪造历史。
- 当前真实截图目录为 `artifacts/1.9.9-build202608041745/screenshots/chart-gap-fill-20260804/`：`03-mini-summary.png`、`05-cpu-tertiary.png`、`07-battery-hover-attempt.png`、`08-battery-tertiary.png`、`10-battery-tertiary-tooltip.png`。`10` 可见 7 天电池柱连续显示，横轴无文字，悬停显示 `8月1日 上午 7:18`、`≈ 100%`、外接电源与“邻近样本补全”。
- 定向回归：`PanelChartNormalizationTests` **51/51**、`GeekSensorsPowerHoverDetailTests` **19/19**；最终全量 XCTest **2267 项执行、10 项按设计跳过、0 failures，54.301 秒**。Release 校准测试 **1/1**，Debug/Release 构建成功。
- 未验证边界不变：真实充放电转换、整个范围无样本、睡眠/唤醒、不同窗口宽度、长时连续录制和所有指标当前 hash 的完整 Hover 视频仍未取得。

## 19. build 202608041959 缺样本尾部与供电来源补全（2026-08-04 20:13 AEST）

- `GeekChartWindow.barBucketSnapshots` 现在从首个真实采样槽生成到当前可见窗口末槽；内部缺桶和最后真实样本之后的尾部缺桶都进入既有 `TimeBucketAggregator.nearestFilled`。补值只存在于显示快照，原始 Store 不变，真实 `0` 不会被当作 missing。
- CPU 堆叠柱会在同一桶内倒序选择最近一条 User/System 同时有效的快照，再执行最近邻补全；标题数字、Tooltip 与柱高继续使用同源快照。
- 电池电量和 `powerSource` 独立补全。电量有效但供电来源为 `nil/.unknown` 时，柱色和外接电源时间线使用最近可信供电来源并标记 `isEstimated`，避免时间线出现假断点；整段都没有可信来源时仍保持 unknown。
- 仍不填充首个真实采样槽之前的暖机区，也不把全范围都无真实值的指标伪造成 0 或平台。
- 定向回归：`PanelChartNormalizationTests` **52/52**、`GeekSensorsPowerHoverDetailTests` **20/20**、`BatteryPresentationTests` **5/5**、`MenuBarTertiaryDetailTests` **6/6**、`SystemMonitorServiceTests` **84/84**。最终全量 XCTest **2277 项执行、13 项环境/显式授权相关跳过、0 failures**。
- Debug 编译、签名和进程启动成功；GUI 会话锁定导致窗口可见性检查失败。Release 校准 **1/1**、应用/DMG/ZIP 构建和 checksum 均通过。
- 当前本机安装为 **1.9.9 (202608041959)**，主程序 SHA-256 `45169187da868b47f4a477e95b4aed5fd5455ddfdee4fbe0f9b40740c349b9aa`，deep/strict 签名通过；旧 build `202608041845` 保存在 `/private/tmp/storage-cleaner-install-backup-202608041959/存储清理助手.app`。未发布 1.9.9。
- 当前 GUI 会话锁定，安装应用进程已启动但窗口数为 0；因此当前哈希的小窗截图、Hover 录屏、浅/深色和 1h/6h 实窗切换仍未取得，旧截图不作为本构建现场证据。

## 20. build 202608042029 统一时钟右边界与当前 Release 实窗复核（2026-08-04 20:54 AEST）

- 电量图原先单独使用最后一条电池样本作为右边界；采样暂停时，“最近 1 小时/7 天”会停在过去。现在电量与电池功率都使用 `GeekLiveChartTimeline` 的共享时钟，右侧始终代表当前可见时刻。
- `usablePoints(endingAt:)` 原先还会用未来的最后样本抬高窗口终点。现在终点固定为共享 `referenceDate`；只容忍 `GeekChartWindow.futureSampleTolerance` 内的轻微时钟偏差并钳制到当前时刻，更远未来样本不进入显示窗口。
- 内部和尾部缺桶继续由 `TimeBucketAggregator.nearestFilled` 仅在显示快照中补齐；真实 Store、真实 `0` 和 missing 语义不变。电量与 `powerSource` 分别补齐，柱色与外接电源时间线继续复用同一个桶和 x。
- 当前本机 7 天持久历史共读取到 2426 条真实电池样本，真实电量范围为 9%–100%，没有 0% 样本。三级图左侧的短柱来自真实 9%–18% 电量，不是白色数据柱；真实缺桶在首个有效样本之后不再形成空列。首个真实样本之前仍不外推，全范围无真实邻居时仍显示不可用，避免伪造过去历史。
- 新增行为回归 `testBatteryRecentWindowKeepsTheSharedClockAsItsRightEdge` 和 `testBatteryRecentWindowRejectsAndClampsFutureSamplesAgainstTheSharedClock`；最终全量 XCTest **2279 项执行、10 项按环境/显式长跑分条件跳过、0 failures，59.412 秒**。最终 Debug bundle-only、Release、校准 1/1、四份归档 checksum 和 deep/strict 签名均通过。
- 当前安装为 **1.9.9 (202608042029)**，主程序 SHA-256 `7ec62bf7ad6500ea941a39c56a53e954502e0a9721548c223b15665289efc63b`。旧 build `202608041959` 可恢复地保存在 `/private/tmp/storage-cleaner-install-backup-202608042029.PYuRu8/存储清理助手.app`；没有发布 1.9.9。
- 当前 Release 实窗证据：`artifacts/1.9.9-build202608042029/screenshots/battery-level2-current-release.png` 显示连续的一小时电量预览、蓝色供电轨和 100% 双圆环；`artifacts/1.9.9-build202608042029/screenshots/battery-level3-history-current-release.png` 显示当前 7 天三级历史、真实蓝/粉状态段以及主/二/三级 5 pt 级联。现场为 100% 已充满且接通外部电源，不能替代真实充放电转换验收。

## 21. build 202608050030 缺样本显示层最终补齐（2026-08-05 01:04 AEST）

- 用户指出的竖向空列确认为真实缺样本，不是 0% 柱，也不是白色数据柱。共享 `TimeBucketAggregator.nearestFilled` 现在用于 CPU、内存、网络、磁盘、电池、GPU、温度与风扇显示桶；首个真实样本之后的内部和尾部缺桶选择时间上最近的真实邻居，等距时选择左侧样本。
- 补值只存在于 `ChartSnapshot`/可见桶，不写回持久 History Store；实测 0 继续绘制为 0，不与 missing 合并。整个范围完全没有真实样本时仍显示不可用；首个真实样本之前不伪造过去历史。
- 电池 `levelPercent` 与可信 `powerSource` 分别按同一桶补齐，随后共用同一 `timeToX`；因此粉/蓝柱切换与蓝色外接电源时间线仍在同一个 x 发生。三级图横轴不绘制标签，时间只由柱子 Hover Tooltip 显示，跨日 Tooltip 继续包含日期与上午/下午。
- 图表定向回归 **74/74 通过**：`PanelChartNormalizationTests` **52/52**、`GeekSensorsPowerHoverDetailTests` **22/22**。最终全量 XCTest **2315 项执行、13 项按环境/显式授权条件跳过、0 failures，48.867 秒**。
- Debug bundle-only 与 Release 均成功；Release DMG/ZIP 校验通过。当前安装为 **1.9.9 (202608050030)**，主程序 SHA-256 `13ad3af5cd50c24f5ba4388614342b6d4e79dd172ef7968ddeb5fc6db77a592e`，deep/strict 签名有效。
- 当前屏幕存在与本任务无关的 `sfltool` 系统管理员授权窗口，菜单栏点击被系统模态拦截；本轮没有替用户批准或关闭。因此当前哈希的二/三级小窗截图仍未取得，旧截图不冒充当前构建证据。

## 22. build 202608050130 已安装实窗缺样本补齐验收（2026-08-05）

- Debug 与 Release 构建均成功；Release 已安装并从唯一安装路径 `/Applications/存储清理助手.app` 运行。本轮完整 XCTest 为 **2316 项执行、13 项按环境/显式授权条件跳过、0 failures**；图表专项已有 **74/74**，其中 `PanelChartNormalizationTests` **52/52**。
- 生产调用链中 CPU、网络、磁盘、传感器和电池均使用 `barBucketSnapshots` + `TimeBucketAggregator.nearestFilled` 在显示层按时间邻近样本补齐；raw History Store 不改写。真实 `0` 仍作为 0 绘制，不与 missing 混淆；首个真实样本之前不外推；整个范围没有有限真实值时仍显示 placeholder。
- 电池 `level` 与 `powerSource` 分别补齐，再由同一组 prepared buckets 和 x 坐标绘制电量柱及供电时间线，因此柱色和时间线同列对齐。
- 已安装实窗截图观察到 CPU 与电池内部柱列连续；三级图横轴不绘制标签。3 天 CPU Hover 显示 `8月4日 上午 12:08`（`artifacts/1.9.9-build202608050130/screenshots/live-installed/04-small-window-cpu-level2.png`、`05-small-window-cpu-level3.png`、`06-cpu-level3-hover-tooltip.png`）；7 天电池 Hover 显示 `8月2日 上午 5:20`、`外接电源`及“邻近样本补全”（`07-small-window-battery-level2.png`、`09-small-window-battery-level3.png`、`10-battery-level3-hover-tooltip.png`）。
- 本轮仅证明当前安装包的实窗连续柱列、Tooltip 文案及电池柱/供电时间线的同列关系；睡眠唤醒与长时现场连续性尚未验证。

## 23. build 202608050252 完整可见时窗邻近补全（2026-08-05）

- 根因是共享 `GeekChartWindow.barBucketSnapshots` 仍从第一个有观测值的槽位开始返回；`nearestFilled` 能补内部和末尾，却看不到首个观测槽之前的可见槽位。现在只要当前可见范围至少存在一个真实观测，快照会返回全部固定槽位，再统一用最近真实邻居补首段、内部和末段；等距继续取左侧邻居。
- 修改只影响显示快照，不回写 `MetricHistoryStore`。真实 `0` 继续保留；整个范围没有任何真实样本时不生成平台，仍显示等待采样。CPU、内存、GPU、温度、风扇、网络、磁盘和电池均复用这一个共享入口。
- 新增前导缺桶行为测试，并将旧的“仅采样开始后返回槽位”断言更新为完整可见槽位语义。`PanelChartNormalizationTests` **53/53**、`GeekSensorsPowerHoverDetailTests` **22/22**；最终全量 XCTest **2317 项执行、10 项按环境/显式条件跳过、0 failures，52.199 秒**。
- Release、生产校准 1/1、两套 DMG/ZIP checksum、DMG verify 和 deep/strict 签名均通过。当前安装为 **1.9.9 (202608050252)**，主程序 SHA-256 `fcb435f8b0269a2f208dccf731e279457e33f6b19d418e01ecdec8da2f614500`；旧 build `202608050130` 保存在 `/private/tmp/storage-cleaner-install-backup-202608050252.0ZGOiz/存储清理助手.app`。未发布 1.9.9。
- 当前实窗证据为 `artifacts/1.9.9-build202608050252/screenshots/live-installed/02-battery-level3-neighbor-fill.png`，放大图为同目录 `02-battery-level3-neighbor-fill-bars-crop2.png`。7 天电池图的每个显示槽均有柱；保留的逐柱空隙是设计中的 1 pt spacing，不是缺样本列。现场持久历史共 2467 条电池样本，未发现 `chargePercent=nil` 或 0% 记录。

## 24. build 202608050252 当前安装版全指标实窗矩阵（2026-08-05 03:35 AEST）

- 从唯一安装路径 `/Applications/存储清理助手.app` 的 Release 补拍当前哈希窗口级 PNG。浅色目录 `docs/screenshots/1.9.9-small-window-charts/build202608050252-light/` 共 15 张，覆盖一级摘要、CPU、内存、网络、磁盘、传感器、温度、风扇、GPU 和电池的二/三级；深色目录 `docs/screenshots/1.9.9-small-window-charts/build202608050252-dark/` 共 3 张，覆盖一级、CPU 三级和电池三级。
- 当前实窗中 CPU/内存/网络/磁盘/电池柱图及 GPU/温度/风扇历史均贯穿可见宽度，没有再出现代表 missing 的整列空白；保留的 1 pt 竖向间距属于既定柱间距。三级图继续不显示横轴文字，时间信息只在柱子 Hover 时出现。
- 当前 CPU、网络、磁盘和电池三级均保持真实 5 pt 父子间隔；矩阵未合成窗口、未使用 Debug fixture。电池现场为 100%、已充满且接通外部电源，因此只能证明蓝色柱/供电轨和缺桶补全，不能证明真实粉蓝切换。
- `menuBar.miniWindowAppearance` 原先不存在。为取得深色证据只临时写入 `dark` 并重启同一安装包，完成后删除该键；`app.appearance` 回读仍为 `system`，应用已从原安装路径重新启动。
- 本节是当前哈希的静态实窗矩阵，不替代睡眠/唤醒、不同屏幕宽度、1h↔6h 操作、充放电转换或当前哈希完整 Hover 录屏。

## 25. build 202608050421 连续数值与离散电源状态分流（2026-08-05 04:49 AEST）

- CPU、内存/压力、网络、磁盘、GPU、温度、风扇、电量和电池功率的缺桶继续统一使用 `TimeBucketAggregator.nearestFilled`，覆盖完整可见槽位；补值只进入显示快照，不写回原始历史，真实 `0` 与 missing 仍严格区分，整段完全没有真实值时不生成假平台。
- 审计发现电池 `powerSource`/`isCharging` 若同样使用最近邻，长缺样跨越插拔电源时会在缺口中点提前切换。新增 `TimeBucketAggregator.forwardFilled`：首个真实状态前使用首个已知状态，之后只保持最近过去状态；因此状态切换只能发生在首个真实新状态桶。电量柱、柱色和 `PowerConnectionTimeline` 仍消费同一组 prepared buckets 与同一 x。
- 新增多槽 `battery → externalPower` 与 `externalPower → battery` 双向测试，断言切换前一槽仍为旧状态、首个真实新状态槽才切换，并验证蓝色时间线的起止边界与柱中心一致。`PanelChartNormalizationTests` **54/54**、`GeekSensorsPowerHoverDetailTests` **23/23**；最终全量 XCTest **2319 项执行、13 项按环境/显式条件跳过、0 failures，49.655 秒**。
- 当前安装 Release 为 **1.9.9 (202608050421)**，主程序 SHA-256 `428edb95d0963fa2747d397c9bea822a0b2dfa471dc975eda86008e4f0f7645e`。CPU 3 天实窗及 Hover 为 `artifacts/1.9.9-build202608050421/screenshots/cpu-level3-installed-safe.png`、`cpu-level3-tooltip-installed-safe.png`；Tooltip 显示 `8月4日 上午 3:01`，三级横轴仍不绘制文字。
- 电池 7 天实窗及 Hover 为 `artifacts/1.9.9-build202608050421/screenshots/battery-level3-installed-safe.png`、`battery-level3-tooltip-installed-safe.png`；可见蓝/粉真实状态段、连续柱列和蓝色供电轨，Tooltip 显示 `8月2日 下午 6:01`。现场为 100% 已充满且接通电源，因此真实双向插拔只由固定行为测试证明，未在本机现场重新制造。
- 仍未扩大到“仅有供电状态、同一时刻完全没有电量值”的独立状态分桶；当前持久现场此前未发现 `chargePercent=nil`，该边界继续列为未验证，不以补值伪造数值柱。

## 26. build 202608050528 当前安装版逐像素连续性复核（2026-08-05 05:45 AEST）

- 当前 Release 已安装并从唯一安装路径 `/Applications/存储清理助手.app` 运行，版本 **1.9.9 (202608050528)**，PID `92617`，主程序 SHA-256 `d48a9810168274ae205f8ea6db64ff280b99aef8f833cb677e711ad087a43236`。旧 build `202608050421` 完整保存在 `/private/tmp/storage-cleaner-install-backup-202608050528.kjB6X6/存储清理助手.app`。
- 当前持久电池历史为 2519 条真实记录，`chargePercent` 范围 **9%–100%**，没有 `nil` 或 0% 记录；三级图中较矮的柱是低电量真实值，不是缺样本空列。缺桶补值仍只发生在可见 `ChartSnapshot`，不回写 `MetricHistoryStore`，真实 0 与 missing 继续分离。
- 对当前安装包窗口截图做原始像素断言：7 天电池图 **120 根**连续柱，每根 **4 px**、柱间 **2 px**，最大相邻空隙 **2 px**，超过 2 px 的空档为 **0**；一级 CPU 与网络图各 **91 根**，结果相同。日志为 `artifacts/1.9.9-build202608050528/logs/chart-pixel-continuity.log`。
- 当前实窗证据为 `mini-window-overview-installed.png`、`battery-level2-installed.png`、`battery-level3-installed.png` 和 `battery-level3-tooltip-installed.png`（同 build 的 `screenshots/` 目录）。三级横轴没有文字；Hover 显示 `8月1日 下午 7:07`、100% 与外接电源。
- 图表、电池 Hover 与风扇安全联合定向回归 **127/127**；安装后最终全量 XCTest **2325 项执行、10 项按显式真实硬件条件跳过、0 failures，51.970 秒**。Debug bundle-only、Release、校准 **1/1**、两套 DMG/ZIP checksum、DMG verify 和 deep/strict 签名均通过。
- 当前包仍是 Apple Development / Team `T9GZL52H8R` 本机测试签名；`spctl` 返回 3，因为没有 Developer ID 公证。没有发布、上传、修改 appcast、提交公证，也没有制造充放电状态或改写历史。睡眠/唤醒、不同显示宽度和当前 build 的长时录屏仍未验证。

## 27. build 202608050653 当前源码与安装边界

- 缺桶补值、离散电源状态 forward-fill、三级横轴无文字和 Hover 时间语义没有在本轮改写；相关实现仍由第 25–26 节的固定 fixture 与逐像素证据覆盖。
- 当前整树全量 XCTest **2334/10 skipped/0 failures**，Debug/Release、生产校准、归档校验与 deep/strict 签名均通过；`/Applications/存储清理助手.app` 已更新为 **1.9.9 (202608050653)**。
- 当前 build 已补拍真实小窗：`artifacts/1.9.9-build202608050653/screenshots/chart-gap-fill-current/mini-summary-current.png`、`cpu-3day-tertiary-current.png` 和 `battery-detail-current.png`。CPU 主摘要、CPU 3 天三级图及电池 1 小时预览均未见代表 missing 的整列空白；电池预览为连续蓝柱，保留的细缝仅为既定柱间距。
- 本次重新执行 `PanelChartNormalizationTests` **54/54** 与 `GeekSensorsPowerHoverDetailTests` **23/23**，0 failures。补齐继续只构造带 `isEstimated/sourceIndex` 的显示快照，真实 `0`、raw History Store 和真实采样时间不变；当前 build 未重复执行逐像素柱间距脚本，因此精确的 **4 px 柱宽 / 2 px 间距 / 0 个超限空档** 断言仍只属于第 26 节 build `202608050528`，不冒充本次现场。

## 28. build 202608050837 当前安装边界

- 图表缺桶规则在本轮没有改写：连续数值仍由 `TimeBucketAggregator.nearestFilled` 只在显示快照中按最近真实邻居补齐，离散供电状态仍使用 forward-fill；真实 `0`、missing、raw History Store 和真实 timestamp 继续分离。
- 当前整树全量 XCTest **2345 项执行、13 项按环境或显式现场条件跳过、0 failures**；Debug/Release、生产校准、归档校验与 deep/strict 签名均通过。`/Applications/存储清理助手.app` 已更新为 **1.9.9 (202608050837)**，主程序 SHA-256 为 `b7e0760aef3f3d5124c4d7d09b7169eee6cd2c86d90a069deb0c50918d7bc525`。
- 当前 App 进程持续运行，但系统回读 `CGSSessionScreenIsLocked=Yes`。因此没有把 build 202608050653 或更早截图当作当前哈希截图；本 build 的浅/深色、全指标二/三级、Hover、1h/6h、睡眠唤醒和长时连续录屏仍列为未验证。
