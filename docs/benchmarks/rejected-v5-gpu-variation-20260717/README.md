# v5 Metal 3D 变异门禁拒绝记录（2026-07-17）

这批记录来自两段独立冷却会话，每段 4 次 Standard Release 跑分，共 8 次。所有单次运行都通过硬件、预检、后检、样本完整性与容量字段校验，但聚合器拒绝冻结为生产基准：8 次 GPU 运行中位数的样本变异系数为 `10.2354%`，超过 `10%` 上限。

- 工作负载指纹：`f4f0f81a9529613769ba207058ea973f6a9c6d4d6a2f5f7e41e208067f567717`
- GPU 中位数范围：`2504.904`–`3090.252 Mtri/s`
- 结论：3 帧未计分 warm-up 不足以稳定 Apple Silicon 的动态 GPU 性能状态；不得通过删除高/低样本来冻结。
- 后续：保留全部原始 JSON，将未计分 warm-up 提高到 180 帧、计分窗口提高到 600 帧，再用新的一次性护栏和新指纹重新执行两段会话。

这些文件仅作为被门禁拒绝的研发证据，不属于任何可发布排行榜基准，也不会被 App 加载。
