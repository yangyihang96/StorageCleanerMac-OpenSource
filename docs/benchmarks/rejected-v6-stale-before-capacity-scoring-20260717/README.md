# v6 容量计分冻结前的失效记录（2026-07-17）

这里原样保留一次尚未完成的 Standard v6 校准尝试：`session-1` 有 4 条通过单次门禁的记录，`session-2` 有 1 条被门禁拒绝的记录。

这批数据不能用于生产基线或排行榜：采集后，v6 评分规则按产品要求加入了内存容量与系统盘容量的有界权重，跑分协议源码指纹因此发生变化；同时两段冷却会话也没有形成至少 `4 + 4` 条有效记录。不得删除异常记录、补写第二段或用旧指纹数据冒充最终校准。

全部 JSON 保持采集时原始字节，仅作为研发审计证据。正式 v6 基线必须在最终 1.8.2 workload、结果处理和评分源码冻结后，使用新的一次性运行护栏重新采集。
